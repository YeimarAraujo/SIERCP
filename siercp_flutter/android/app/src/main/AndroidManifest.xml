<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos de almacenamiento y red -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- CÁMARA — requerido para mobile_scanner (QR) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- NOTIFICACIONES — requerido en Android 13+ (API 33) para FCM/local -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- BLUETOOTH — Requerido para flutter_blue_plus -->
    <!-- Android 11 y anteriores -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Android 12 y posteriores -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:label="Sicap"
        android:name="${applicationName}"
        android:icon="@mipmap/logo">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Ícono por defecto para notificaciones FCM (usa el launcher del app).
             No fijamos un channel_id propio: si no existe, el SDK de Firebase
             crea un canal por defecto automáticamente, así no hace falta código
             nativo ni paquetes extra para mostrar la notificación. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/launcher_icon" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Desactiva Impeller (Vulkan): algunas GPU Mali tienen drivers Vulkan
             defectuosos que provocan pantalla en blanco y "Lost connection to
             device". Con esto se usa el render clásico (Skia/OpenGL). -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>